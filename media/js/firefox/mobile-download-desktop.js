(function($) {
    'use strict';

    var form = new Mozilla.SendToDevice();
    form.init();

    $('#send-to-device-form').on('submit', function() {
        // ye olde GTM tracking
    });
})(window.jQuery);
